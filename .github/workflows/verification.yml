name: Source migration in progress
on: workflow_dispatch
permissions: {contents: read}
jobs:
  migration:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Gemma source migration branch only; this is not a verification result.'
